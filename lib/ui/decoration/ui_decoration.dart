import 'package:flutter/material.dart';

class UiDecoration {
  static const cardInfoDecoration = BoxDecoration(
    color: Colors.white,
    borderRadius: BorderRadius.all(Radius.circular(15)),
  );
}

class UiDecorationColors {
  static const defaultAppColor = Color.fromRGBO(242, 242, 247, 1);
}
